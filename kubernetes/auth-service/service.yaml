apiVersion: v1
kind: Service

metadata:
  name: auth-service
  namespace: kd
  annotations:
    argocd.argoproj.io/sync-wave: "2"

spec:
  selector:
    app: auth-service

  ports:
  - port: 8081
    targetPort: 8081

  type: ClusterIP
