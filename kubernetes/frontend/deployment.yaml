apiVersion: apps/v1
kind: Deployment

metadata:
  name: frontend
  namespace: kd

spec:
  replicas: 1

  selector:
    matchLabels:
      app: frontend

  template:
    metadata:
      labels:
        app: frontend

    spec:
      containers:
        - name: frontend
          image: kdmodder/frontend:1.0.1
          imagePullPolicy: Always

          ports:
            - containerPort: 3000

          resources:
            requests:
              cpu: "50m"
              memory: "128Mi"

            limits:
              cpu: "500m"
              memory: "512Mi"
