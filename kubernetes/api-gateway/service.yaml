apiVersion: v1
kind: Service

metadata:
  name: api-gateway
  namespace: kd
  annotations:
    argocd.argoproj.io/sync-wave: "2"

spec:
  type: LoadBalancer

  selector:
    app: api-gateway

  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
