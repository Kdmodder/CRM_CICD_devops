apiVersion: v1
kind: Service

metadata:
  name: api-gateway
  namespace: kd
  annotations:
    argocd.argoproj.io/sync-wave: "2"

spec:
  type: NodePort

  selector:
    app: api-gateway

  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
