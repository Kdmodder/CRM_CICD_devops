apiVersion: v1
kind: Service

metadata:
  name: support-service
  namespace: kd

spec:
  selector:
    app: support-service

  ports:
    - name: http
      port: 8085
      targetPort: 8085
      protocol: TCP

  type: ClusterIP
