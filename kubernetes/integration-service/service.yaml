apiVersion: v1
kind: Service

metadata:
  name: integration-service
  namespace: kd

spec:
  selector:
    app: integration-service

  ports:
    - name: http
      port: 8088
      targetPort: 8088
      protocol: TCP

  type: ClusterIP
